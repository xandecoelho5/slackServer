const express = require('express');
const slashController = require('./../controllers/slashController');

const router = express.Router();

// router
//   .route('/')
//   .post(slashController.slashData);

// router
//   .route('/:id')
//   .get(slashController.getBooking)
//   .patch(slashController.updateBooking)
//   .delete(slashController.deleteBooking);

module.exports = router;